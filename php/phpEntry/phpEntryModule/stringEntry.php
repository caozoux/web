<?php 
class stringEntry {

	public function deleteSpaceChar () {
	/*string rtrim ( string $str [, string $character_mask ] )
	该函数删除 str 末端的空白字符并返回。 
	不使用第二个参数，rtrim() 仅删除以下字符： 
	◦ " " (ASCII 32 (0x20))，普通空白符。  
	◦ "\t" (ASCII 9 (0x09))，制表符。  
	◦ "\n" (ASCII 10 (0x0A))，换行符。  
	◦ "\r" (ASCII 13 (0x0D))，回车符。  
	◦ "\0" (ASCII 0 (0x00))，NUL 空字节符。  
	◦ "\x0B" (ASCII 11 (0x0B))，垂直制表符。 */

		echo saf, trim(" charlisti ")
		echo saf, ltrim(" charlisti ")
		echo saf, rtrim(" charlisti ")
	}
	public changSlotAndUpload () {
		echo strtoupper("test string")
		echo ucfirst("test string")
		echo ucwords("test string")
	}
}
?>
